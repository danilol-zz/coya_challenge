<div class="uk-width-1-1 uk-width-1-2@m">
  <%= image_tag "weather_icons/#{@report.weather[:main].downcase}.svg" %>
</div>

<div class="uk-width-1-1 uk-width-1-2@m">
  <% if @report.success? and @info.success? %>
    <div class="city-country"><%= @report.city %>, <%= @info.name %></div>
    <div class="degrees"><%= @report.weather[:temp] %> deg</div>
    <div><strong>Currency:</strong> <%= @info.currencies.first["code"] %></div>
<% else %>
  <%= @info   unless @info.success? %>
  <%= @report unless @report.success? %>
<% end %>
</div>
